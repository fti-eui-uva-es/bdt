<? php
$errorsArray = array();
function appendError($error) {
	global $errorsArray;
	$ERRORS = array(NULL, "Contraseñas incorrecta");
	array_push($errorsArray, array(
		"error" => $error,
		"msg" => $ERRORS[$error]
	));

if (isset($_POST['nombre']){
	header(content-Type: apllication/json);
	$nombre=$_POST['nombre'];
	$passwd=BD:: query("SELECT user_password FROM user WHERE user_nick=$nombre");
	if ($passwd==$_POST['pass'] {
		header("Localization: //explicitar localizacion")
	}else{
		appendError(1);		
	}
}
?>
	
